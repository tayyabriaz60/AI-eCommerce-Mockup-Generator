DATABASE_URL=
# Optional — anonymous ZeroGPU works; a token raises your free daily quota.
HF_API_TOKEN=
# Hugging Face Space repo id (ZeroGPU public Space).
HF_MODEL=black-forest-labs/FLUX.1-Kontext-dev
# Max seconds to wait in queue + generate (ZeroGPU can be slow).
HF_SPACE_TIMEOUT_SECONDS=120
PORT=8000
